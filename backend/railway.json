{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run build && npx prisma migrate dev --name init && npm run db:seed && npm start",
    "restartPolicyType": "ON_FAILURE"
  }
}