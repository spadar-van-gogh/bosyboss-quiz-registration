{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run build && npx prisma migrate reset --force && npx prisma migrate dev --name init && npm start",
    "restartPolicyType": "ON_FAILURE"
  }
}